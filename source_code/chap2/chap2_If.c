#include <stdio.h>

int main() {
  int number_A = 5000;

  if (number_A >= 5000){
    printf("number_A가 %d 보다 크다!!\n", number_A);
  }
  else {
    printf("number_A가 %d 보다 작다!!\n\n", number_A);
  }
  return 0;
}